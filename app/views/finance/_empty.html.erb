<div class="finance-empty"><%= ui_icon(:wallet, css: "size-9") %><h3><%= local_assigns.fetch(:title, "ยังไม่มีรายการ") %></h3><p><%= local_assigns.fetch(:description, "เอกสารในแปลนบ้านที่เลือกจะแสดงที่นี่") %></p></div>
