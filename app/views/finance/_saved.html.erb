<div class="finance-success"><span class="success-mark"><%= ui_icon(:check) %></span><p class="eyebrow">REQUEST SUBMITTED</p><h2>บันทึก <%= document_number(document) %> แล้ว</h2><p>คำขออยู่ระหว่างรออนุมัติ ยอดงบประมาณยังไม่ถูกหัก</p><div class="flex flex-wrap justify-center gap-3 mt-6"><%= link_to "ดูรายละเอียดเอกสาร", polymorphic_path(document), data: { turbo_frame: "_top" }, class: "btn btn-primary" %><%= link_to "กลับภาพรวม", dashboard_path(workspace_params), data: { turbo_frame: "_top" }, class: "btn btn-secondary" %></div></div>
